# SPDX-FileCopyrightText: 2026 Unpxre (GitHub: UnpxreTW)
# SPDX-License-Identifier: Apache-2.0

name: Build

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v7
      - name: Pin Xcode 26.3
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '26.3'
      - name: 逐包步驟漂移警報（package 數 vs step 數）
        run: |
          packages=$(ls -d Packages/*/Package.swift | wc -l | tr -d ' ')
          steps=$(grep -cE '^ +run: swift build --package-path Packages/' .github/workflows/build.yml)
          echo "packages=$packages steps=$steps"
          if [ "$packages" != "$steps" ]; then
            echo "::error::Packages/ 有 $packages 個套件，本 workflow 只列了 $steps 個逐包步驟——新增套件時請一併補步驟"
            exit 1
          fi
      - name: swift build（MachineKit）
        run: swift build --package-path Packages/MachineKit
      - name: swift build（NymphKit）
        run: swift build --package-path Packages/NymphKit
      - name: swift build（mayfly-cli）
        run: swift build --package-path Packages/mayfly-cli
      - name: swift build（LinuxNodeKit）
        run: swift build --package-path Packages/LinuxNodeKit
      - name: swift build（MayflyUI）
        run: swift build --package-path Packages/MayflyUI
